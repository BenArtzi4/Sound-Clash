# UI redesign — final validation, 2026-09-22

Mode: **full** (tasks live: T0 … T11; skipped tags: none). Prod build: `index-TBFd5Vnn.js` (main @ `6829298`, PR #327). Prod window (UTC): 2026-09-22 04:15 – 05:50 (games `ETY4ZR`, `VVJXDE`, `NBUM9Q`, `J4XYYG`, the 11 rate-limit creates, `9GE8C2`, `35RE2G`, `QPGQFC`, `3RMQJB`, `PXFRNX`, `PCFBJW`, `2BF3DF`, `VDT92D` — all ended, confirmed at 05:57Z). Session model: Fable 5.1 (subagents Opus).

## Verdict: **GO** (updated after the merges — the device checks below are still owed)

_Update 2026-09-22 06:50Z: the maintainer authorised merging in-session; all six fix PRs are merged and deployed as `main @ f5e37d8`, and the post-merge re-verification (section below) is clean. The paragraph that follows is the pre-merge verdict; its two merge conditions are now met. What remains owed is the device checklist, above all the real-iPhone Home → Host check that decides whether the WebKit transition skip (#329) stays._

Pre-merge verdict, kept for the record: **GO — conditional on two merges and one device check.**

Every behaviour gate is green: 601 vitest, 390 + 1 backend, 40 + 16 local e2e on Chromium and 5 on Firefox, the 44-case prod API + RPC matrix, the 30-team protocol game (0 violations, 0/1888 Realtime misses, 32/32 sockets), and three real-browser 13-team games on prod in which every button and combination behaved per `docs/game-rules.md`, scores agreed across host, phones, Display and the DB at every step, every role stayed free of app console errors, and the buzz path measured 2.0 ms to `pending` and 3.0 ms to the RPC. The design is on screen as `01` / `04` / `05` / `09` specify on every route and state that was looked at. Three P1s were found, each with a merge-ready fix PR: WebKit crashed the page on the route transition into the lazy Host / How-to-play screens (**#329**, the hook bails out on WebKit), small red text at 4.40:1 on the console's Wrong button (**#330**, new `--negative-text`), and a missing tap-highlight reset on the console's toast close (**#331**). Three unambiguous P2s have fix PRs too (**#332** hover guard, **#333** YouTube cover, **#334** Home link tap target). Nothing open is a P0. The GO holds once #329 and #330 are merged and re-verified (list below); the WebKit fix is deliberately conservative and should be reverted if the maintainer's real iPhone / macOS Safari check (device list) shows the transition surviving there. F-08 / F-09 stay as recommendations; F-03 / F-10 / F-11 are issues #335 / #336 / #337.

## Findings

| id | sev | where | route / state | symptom | evidence | spec | proposed fix | status |
|---|---|---|---|---|---|---|---|---|
| F-01 | P2 | A.2 | Console, player cover (loading / song ended / unavailable) | `linear-gradient(135deg, #1e293b, #0f172a)` with old-palette text colours (`rgba(255,255,255,.7)`, `#fecaca`) and a fifth easing `ease` | `YouTubePlayer.module.css:19-31,40,45` | 01 §2 "No gradients, anywhere", §2.1 tokens; 04 §2 four easings | flat `--surface-2` cover, `--text-muted` / `--bone` text, `--dur-state --ease-move` | **PR #333** (`CLEAN`, labelled e2e green) |
| F-02 | P2 | A.2 | every page (`a:hover`), `/admin/songs` (`tr:hover`) | two `:hover` rules outside `@media (hover: hover)` — a tapped link stays underlined on touch | `hover-guard.mjs`: `styles.css:149`, `AdminSongsPage.module.css:87` | 07 Global Constraints; 05 §0 buttons "Hover only under (hover: hover)" | wrap both in `@media (hover: hover)`; add a vitest hover guard | **PR #332** (`CLEAN`, labelled e2e green) |
| F-03 | P3 | C.3 | test harness | `tests/e2e/smoke/prod_realtime.spec.ts` `deriveApiBase()` maps only the apex host, so `BASE_URL=https://www.soundclash.org` derives `api.www.…` and the cleanup leaves the game running | `getaddrinfo ENOTFOUND api.www.soundclash.org`; game `NBUM9Q` ended by hand | not redesign | strip `www.` in `deriveApiBase`, or document `API_URL=` in 08 C.3 | issue #335 |
| F-04 | **P1** | B.3 | Home → Host, Home → How to play on WebKit | **page crashes** on the view transition into a lazy route taller than the viewport (WebKit 26.5, 30/30 local + prod); reduced-motion / no-API path 0/12 | scratch `webkit-vt-probe*.mjs`; e2e `full_game` + `mobile_team` on `--project=webkit` | 04 §4 route transitions; 06 §4.1 zero errors on every route | hook bails out on `navigator.vendor === "Apple Computer, Inc."` (instant swap, the spec's own fallback) until a real device proves Safari clean | **PR #329** (`CLEAN`, labelled e2e green) — real-device check owed |
| F-05 | **P1** | E.1, E.2 | Home "How to play →" link; console toast close `×` | no `-webkit-tap-highlight-color` reset → browser tap flash (`rgba(51,181,229,.4)` on Android, `rgba(0,0,0,.18)` desktop) | audit `hover-touch` lines at `/` and on every console state | 06 §4.1 (`rgba(0,0,0,0)` on controls), 05 §4 tap-flash fix | one global rule on `a, button, input, label, [role="button"]` + vitest | **PR #331** (`CLEAN`, labelled e2e green) |
| F-06 | P2 | E.1 | Home "How to play →" link (phone) | 111×24 px tap target | audit `tap-targets` at iPhone 12 and 360×640 | 06 §6 ≥ 44×44 on phones; E.1 table `tap-targets` | `min-height: 44px` on the link (inline-flex, text unchanged) + e2e guard | **PR #334** (`CLEAN`, e2e guard 24 → pass, labelled e2e green) |
| F-07 | **P1** | E.1, E.2 | Console Wrong button label + points; How-to-play `−3` chip; Display low countdown; `.error` strips; rescue/host-link "copy failed" | `--negative` measures **4.40:1** on `--surface` and **3.94:1** on `--negative-soft` — 01 §2.1's "4.7:1" was computed on the pre-09 surface | audit `contrast` lines; `styles.test.ts` | 06 §6 ≥ 4.5:1 body text; 01 §2.1 | new `--negative-text: #ff5a4f` for small red text (≥ 4.6:1 on every ground, asserted), fills/borders keep `--negative` | **PR #330** (`CLEAN`, local + labelled e2e green) |
| F-08 | P3 | E.2 | Display board, the **buzzed** row | rank numeral drawn in `--accent` on the `--accent-soft` row at 5.87:1 | `.bigRowBuzzed .bigRank { color: var(--accent) }` | 01 §2.2 "`--bone` numerals" (buzzed row unspecified); 06 §6 7:1 is for scores/names | keep (display-size numeral, ≥ 3:1) or switch to `--bone` for the 7:1 floor — taste | recommendation |
| F-09 | P3 | E.2 | Final Results (all roles) | `h2` "Top teams" / "Final standings" styled as a `--text-caption` label in the UI face, not the display face | audit `fonts: H2 …` on `ended` | 01 §3.2 h2 = Anton; §3.2 also defines the caption role for section labels | keep the caption look but render it as `<p>` (or accept h2-as-caption); no user-visible change | recommendation |
| F-10 | P3 | D.5 | player + display + console tabs during the 13-tab games | 4 `stale_buzz_lock_resynced` backstop heals (2 per game; 0 expected by the plan) — a Realtime lock event missed and repaired within 15 s, never a stuck buzzer | Loki 05:02:06, 05:02:42 (`QPGQFC`), 05:17:36, 05:18:04 (`PXFRNX`) | not redesign (hooks untouched; #254/#259 design) | glance at fan-out under many browser tabs; the load harness's 32 sockets saw 0/1888 misses | issue #336 |
| F-12 | P3 | E.4 (post-merge review) | Join `/join`, `/join/<CODE>` (and Host create, Display entry) | 05 §2 says "Wordmark small at top"; the built Join page renders no `<Logo>` (nor do Create and the Display entry), and 07 records no departure | `JoinTeamPage.tsx` has no `Logo` import; `static-_join_ABCDEF-iphone12.png` | 05 §2 | either add `<Logo size="small">` above the Join / Create / Display-entry headings or amend 05 §2 to match the shipped page — taste (09 already removed Home's small wordmark) | recommendation |
| F-11 | P3 | E.4 | Console status strip while a buzz is held | "FV-03buzzed in — score it:" — no visible gap between the display-face team name and the verb | `host-buzzed.png`, `host-buzzed-390x844.png`; JSX has a literal space | 05 §5 copy | `margin-inline-end: 0.3em` on `.lockedTeam` | issue #337 |

Non-findings recorded so the next run does not re-triage them: `--accent-ink: #000` shorthand; five finished 0.01 ms `rise` animations under reduced motion; `ten_teams_thirty_rounds` capping itself at the 7-song local pool; B.2's 16 (= 8 × 2, the plan said 9); the load run's advisory `buzz_in` p95 WARN; FV-13's harness-aborted requests and the browser's own 404/410 console lines; the plan's S7 / S20 / `???`-era expectations corrected above; the manifest's `#14120e` (09/#327 supersede the plan's `#000000`).

## Post-merge re-verification (2026-09-22, main @ `f5e37d8`)

The maintainer authorised merging every fix PR in-session. Merged in severity order as squash commits: #329 `9b35b2d` → #330 `eb378a5` → #331 `6a309f6` → #332 `ab3a370` → #334 `ba7562a` → #333 `f5e37d8` (each later branch had `main` merged in and its changelog / `styles.test.ts` conflicts resolved; lint + type + test and CodeQL were re-run green after every conflict push). Main's own `Frontend` (deploy) and `E2E` runs on `f5e37d8` both `success`; the deployed `index-BcLi3DMk.js` equals a fresh local build of `main`. Three Opus subagents then re-verified prod in parallel:

- **Real-browser game + API matrix** (game `6NQUWX`): S1–S19 PASS, S20 PASS on every product assertion (heading, exports, final scoreboard, DB `ended`, join after end) with two harness-only misses — the script still asserted `ConfirmDialog`'s default body, and the game ended in a 20–20 tie so the podium correctly rendered **`WINNERS`** (EndScreen's tie behaviour, #180) against an exact `WINNER` match. Both assertions corrected in the scratch harness. **The fixes are visible in the audits: 0 `contrast:` lines on any host state (was 4.40:1 on the Wrong button), 0 `hover-touch:` lines (was the toast close)**; only the two accepted remarks remain (buzzed-row rank numeral 5.87:1, "Top teams" caption h2). Console `errors=0 csp=0 failed=0` on host, stranger, display, FV-01…06, host-recovered, FV-07-rescued, FV-08-again (FV-13 / badcode / late-join show only the documented benign lines). Latency `pointerdown→pending 4.2 ms`, `→buzz_in request 4.6 ms`, rtt 81 ms. `api-matrix.sh` → `ALL PASS (44 cases)`. Leftovers 0; DB scores match the ledger for all 12 teams.
- **Prod deploy + design audits**: 11/11 deployed chunk hashes equal the local build of `f5e37d8` (`index-BcLi3DMk.js` byte-identical at 232,778 B; lazy chunks `DisplayPage-8I1QuneX`, `ManagerConsolePage-BeNIADsh`, `TeamGameplayPage-dbzZNDUP`, `useGameChannel-P6Xu1xfQ`); `startViewTransition` still once in the index; fonts `font/woff2` + immutable. `ui_prod_pass.mjs all` → **29/29**, exit 0 (game `87J5SK` ended); its two SOFT notes are the documented stale `(Task 4)` reduced-motion labels (5 finished 0.01 ms animations listed, 0 moving). Static audit on Chromium: **zero `VIOLATION` lines** across all 118 route × viewport checks (the pre-merge run had 36: `hover-touch`, `tap-targets`, the `−3` chip contrast, the `#000` token string, the reduced-motion count) — F-05, F-06 and F-07's How-to-play half are gone from prod. Sweeps on the merged tree: `hover-guard OK`; `gradient(` only in `EndScreen.module.css:135`; `YouTubePlayer.module.css` free of the slate hexes. Leftovers 0.
- **WebKit / Firefox on the merged tree** (`sc-validation` worktree at `f5e37d8`, local stack): WebKit `full_game` + `mobile_team` + `display_fit` → **5 passed** (a first attempt lost `full_game` to the local-Realtime round-advance flake — the DB held round 3 while the console header lagged on round 2 with "Loading next round…" up; passed alone and on a clean full re-run; no `Page crashed`, no `startViewTransition`, no font/CSS error), Firefox → **5 passed**; prod WebKit probe (`webkit-vt-probe.mjs`) → **12/12 `ok`, no CRASH**, every Host row `checkboxes: 18` (pre-fix: crash on every Host / How-to-play row); `webkit-vt-probe10.mjs` → no CRASH in any variant (its `pseudo animations none` rows print `exc` because prod's CSP has no `style-src`, so Playwright's `addStyleTag` is refused before the click — a probe artefact, `crashed=false`); Chromium `redesign_guards.spec.ts` → 3 passed, Home → Host still starts a transition on Chromium and the new 44 px tap-target guard passes. F-04 is closed on the engine that reproduced it; the real-device check remains the maintainer's.

## Re-verify after merge (as planned before the merges — every item below was done in the post-merge section above: WebKit 5/5 + probe 12/12; static audit 0 violations incl. no `contrast:` / `hover-touch:` / `tap-targets:` lines; console game audits free of `contrast:` and `hover-touch:`; `hover-guard OK` and the gradient sweep down to the EndScreen; `ui_prod_pass` 29/29)

- **B.3 WebKit** — after PR #329 deploys: `cd tests/e2e && npx playwright test full_game.spec.ts mobile_team.spec.ts display_fit.spec.ts --project=webkit --retries=0` must pass 5/5 (it was 3/5), and the scratch `webkit-vt-probe.mjs https://www.soundclash.org webkit` must print `ok` on every row.
- **E.1 + E.2 contrast** — after PR #330 deploys: re-run the static audit against prod (no `contrast:` line on `/how-to-play`) and one console `buzzed` audit (no `contrast:` on the Wrong button); `node tests/smoke/ui_prod_pass.mjs all` stays 29/29.
- **E.1 hover-touch / tap-targets** — after PRs #331 and #334 deploy: the static audit prints no `hover-touch:` / `tap-targets:` line at `/` and no `hover-touch:` on the console states.
- **A.2 sweeps** — after PRs #332 and #333 deploy: `hover-guard.mjs` prints `hover-guard OK`; the `gradient(` sweep lists only the EndScreen winner sweep.
- **E.0** — `node tests/smoke/ui_prod_pass.mjs all` (sandbox disabled) after the last of the six merges, expect 29/29.

## Owed to the maintainer

```
- [ ] iPhone (Safari 17/18): /join → /team → one buzz race, one Correct Song, one Wrong, End → podium. Check: dvh fill, no tap flash, black label on orange legible outdoors, fonts load, View Transitions or instant swap with no glitch.
- [ ] Android Chrome (mid-range): same flow; the idle pulse stays smooth (60 fps) with the phone at 50 % brightness; INP on the BUZZ press feels instant.
- [ ] Samsung Internet: /team only — buzz works, tones correct.
- [ ] TV / laptop → HDMI Chrome 1920×1080, from 3 m: code chip and rank numerals readable, QR scannable from a phone, board reorder visible, podium readable.
- [ ] Firefox desktop: / → Host → Play → Display: instant swaps, no blank frame, no console error.
- [ ] Two real phones in one game (06 §9): both join by QR, race, score every button once, soundtrack round, Keep playing +1h (only if within 20 min of expiry), End → podium on both, Export on the host.
- [ ] Chrome "Install app" still offered on prod (PWA unchanged).
```

## Part 0 — setup

- **0.1** Report branch `feature/ui-final-validation-report` from `origin/main` @ `6829298`. (`main` itself is checked out by a leftover worktree at `C:/Users/yulin/GBA/sc-validation`, clean and empty, so the branch was cut from `origin/main` directly.)
- **0.2 Fingerprints** (all twelve live → full mode):

| Task | Fingerprint | Result |
|---|---|---|
| T0 fonts | woff2 files in `public/fonts` | 4 (anton-latin, instrument-sans-latin, secular-one-hebrew, heebo-hebrew) |
| T1 tokens | `--accent: #FF7A00` in `styles.css` | present as lowercase `--accent: #ff7a00` (the fingerprint grep is case-sensitive; the token is live at line 27) |
| T2 icons | `src/components/icons.tsx` | present |
| T3 wordmark | `EqualizerMark` in `Logo.tsx` | 2 |
| T4 home copy | `Name the song` in `HomePage.tsx` | 1 |
| T5 code field | `src/components/GameCodeField.tsx` | present |
| T6 create | `scroll-snap` in `ManagerCreateGamePage.module.css` | 3 |
| T7 buzz screen | `data-round-live` in `TeamGameplayPage.tsx` | 1 |
| T8 console | `--accent` in `ManagerConsolePage.module.css` | 9 |
| T9 display | `@formkit/auto-animate` in `package.json` | 1 |
| T10 transitions | `src/hooks/useViewTransitionNavigate.ts` | present |
| T11 cleanup | `--space-xs` in `styles.css` | 0 |

  Merged redesign PRs on `main`: #303 #304 #307 #308 #309 #310 #311 #312 #313 #314 #315 #316 #320 #323 #324 #326 #327.

- **0.3 Prod serves the merged tree.** Deployed eager chunks: `index-TBFd5Vnn.js`, `index-B_7KuYU6.css`, `vendor-fHMnGTVz.js`; lazy: `DisplayPage-DLuFfpnK.js` / `-BYeJ2BG_.css`, `ManagerConsolePage-Bx55kyZG.js` / `-1feokujN.css`, `TeamGameplayPage-Dar2IVVN.js` / `-bGrUi-Nq.css`, `useGameChannel-DAmMvcMI.js` / `-CjH373gG.css`. Local build (`npm ci && npm run build` on `6829298`): **every one of the 11 hashes above is byte-identical** to the deployed set (eager + lazy), so Parts C–E ran against the merged tree. Note: the first `npm ci` failed silently inside the Bash sandbox (registry egress blocked) and a second attempt hit a Windows file lock held by a `vite preview --outDir dist-branch` process left running since 2026-09-20 (PID 39228, port 4173); that stale preview was stopped before the install.
- **0.4 Local stack.** `supabase start` already up (CLI 2.117.0); DB at migration 047 (`award_attempt` single overload, `set_song_availability`, `team_secrets`, `game_secrets`, `total_rounds` gone), seed catalog 13 songs / 12 `song_genres` / 11 genres (the 13th song is the harmless `measure0001` leftover noted in lessons-learned 2026-09-20); Realtime publication carries all three ephemeral tables; `MigrationCountMismatch` count in the Realtime log = 0. `tests/e2e/.env` points at 127.0.0.1:54321 / localhost:8000; Playwright 1.61.1 with chromium-1228, firefox-1532, webkit-2311 installed.
- **0.5 Prod prerequisites.** `/health` → `{"status":"ok","supabase":"ok"}` at 04:15:29Z; `VITE_SUPABASE_URL=https://jvfddxuaqcsrguibkymp.supabase.co` + anon key present in `frontend/.env.production`; live games (`status='playing' and expires_at > now()`) = **0**; no non-ended games at all. Grafana MCP answers (`grafanacloud-logs` Loki, `grafanacloud-traces` Tempo).

## Part A — automated gates

- **A.1 Frontend gate** — all exit 0: `format:check` (Prettier clean), `lint` (eslint clean), `typecheck` (tsc clean), `test:run` **600 passed / 59 files** (baseline 521 → +79, nothing removed), `test:coverage` **93.66 % lines / 86.27 % branches / 94.6 % functions / 97.1 % statements** (thresholds 85/80/85/85 met), `build` ends with `check-bundle: 20 chunks scanned; "@formkit/auto-animate" only in DisplayPage-DLuFfpnK.js`.
- **A.2 Source sweeps** (ripgrep 14.1.1 over `frontend/src`):
  - emoji: 0. `transition: all`: 0. `backdrop-filter`: 0 (one comment only). "Welcome to": 0. `how-to-play-hero.png`: 0 refs and the file is gone. Legacy aliases (`--space-xs|--easing-spring|bg-drift`): 0 in source (the one hit is `styles.test.ts:118`, a `not.toContain` guard). `startViewTransition`: exactly `hooks/useViewTransitionNavigate.ts`. `auto-animate`: exactly `pages/DisplayPage.tsx`.
  - `gradient(`: `EndScreen.module.css:135` (the winner light sweep — allowed) **and `components/YouTubePlayer.module.css:19`** (`linear-gradient(135deg, #1e293b 0%, #0f172a 100%)` on the console's player cover, with the old slate palette, `rgba(255,255,255,.7)` text, `#fecaca` error text and a fifth easing `ease`) → **F-01**. The plan's "code-field cells" allowance is obsolete: PR #317 rebuilt the field as a grid and it no longer uses a gradient.
  - `box-shadow`: `--shadow-modal` on Toast, ConfirmDialog, TeamRescueModal and PointChange (the pill's shadow is specified by 07 Task 3); the rest are `box-shadow: none` resets or **inset** 3 px rules (rank-1 accent bar on the board and podium, the code-field caret stand-in) which are lines, not elevation. No finding.
  - `translateY(-6px)|rotate(`: only the two submit-spinner keyframes (`JoinTeamPage.module.css:64`, `ManagerCreateGamePage.module.css:306`, `rotate(360deg)`) — 04 §5 row 6 allows spinner rotation. No finding.
  - `hover-guard.mjs`: **2 `:hover` rules outside `@media (hover: hover)`** — `styles.css:149 a:hover { text-decoration: underline }` (global, reaches every page) and `AdminSongsPage.module.css:87 .table tbody tr:hover` → **F-02**.
- **A.3 Fonts** — 4 woff2, **68,724 bytes** total (anton-latin 18,612 · instrument-sans-latin 30,092 · secular-one-hebrew 7,984 · heebo-hebrew 12,036; ≤ 120 KB ✓). `_headers` has `/fonts/*  Cache-Control: public, max-age=31536000, immutable`; `index.html:19` preloads `anton-latin.woff2` (`as="font" type="font/woff2" crossorigin`); `fonts.css` declares Anton, Instrument Sans, Secular One, Heebo, all `font-display: swap` with `unicode-range` subsets.
- **A.4 Bundle vs baseline (gzip)** — `index-TBFd5Vnn.js` **62.70 KB** (baseline 60.94, Δ +1.76), `vendor-fHMnGTVz.js` **74.14 KB** (Δ 0), all CSS chunks **19.61 KB** (baseline 20.93, Δ −1.32). Eager JS +1.76 KB against a +20 KB budget; CSS under baseline. Chunk guard: `check-bundle.mjs` (sourcemap-based) confirms `@formkit/auto-animate` lives only in `DisplayPage-*.js`; no `framer-motion`/`lenis`/`gsap` anywhere (0 hits in the deployed `index`, `vendor` and all lazy chunks). The literal grep of the deployed chunks for `auto-animate` returns 0 even for DisplayPage — Rolldown minifies the package name away (lessons-learned 2026-09-19), which is why the guard reads sourcemaps.
- **A.5 Backend + DB gate** (throwaway testcontainer, `DATABASE_URL=""`): `ruff check` clean, `ruff format --check` 28 files formatted, `mypy app` no issues in 28 files; `pytest -m "not stress"` **390 passed, 1 skipped, 1 deselected** in 222 s (no `test_rls_anon` rerun needed); `pytest -m stress` **1 passed** (the 10-concurrent → 1-winner race, 100×) in 14.5 s.
- **A.6 CI on `main` @ `6829298`** — `Frontend` success, `E2E` success, `CodeQL` success (all 2026-09-21T14:47Z); `Backend` is path-filtered and last ran on `3d4b73b` (2026-09-20, success) — no backend file changed since. The `E2E` run for the previous SHA `f9f00f0` shows `cancelled` only because the #327 merge superseded it (concurrency group).

## Part B — local e2e

Against the local `supabase start` stack (Playwright 1.61.1, `--retries=0`, the config's own uvicorn + Vite dev server, sandbox disabled so the console can reach `www.youtube.com`).

- **B.1 batch 1** (`buzzer_race full_game multi_buzz_round wrong_buzz_recovery token_claim_constraints`, chromium): **14 passed (3.8 min)**, 0 failed, 0 skipped.
- **B.1 batch 2** (`bonus_flow soundtrack_playthrough reconnection team_rejoin host_recovery expiration`): **10 passed (2.1 min)**, 0 failed, 0 skipped.
- **B.1 batch 3** (`display_fit mobile_team manager_cleanup_yt_csp buzzer_realtime_drops admin_songs_crud redesign_guards`): **13 passed (1.7 min)**, 0 failed, 0 skipped — `display_fit` green at all ten viewports for 4 / 10 / 16 teams on the 8+4 grid; `redesign_guards` confirms no non-passive touch/wheel listeners on the buzz screen, same-frame buzz feedback, and no view transition covering the arrival at `/team/:code`.
- **B.1 batch 4a** (`four_teams_twenty_rounds`): **1 passed (21 s)** — live scoreboard, podium and the durable `game_history` archive all correct.
- **B.1 batch 4b** (`ten_teams_thirty_rounds`): **2 passed (46 s)** — the spec's own guard capped the run at 7 rounds (`pool=7`: the local seed catalog holds 7 songs in its genres; scenarios beyond that are skipped by the spec, not by Playwright), the DB was verified after every round, the soundtrack round scored +15 with the film reveal.
- **B.1 total: 40 passed, 0 failed, 0 skipped** across the 18 baseline specs + `redesign_guards`.
- **B.2 hot-path repeat** (`buzzer_race multi_buzz_round buzzer_realtime_drops --repeat-each=2`): **16 passed (2.9 min)** — 8 tests × 2 (the plan's "9 × 2" over-counts: the three specs hold 1 + 5 + 2 tests). No flake on the buzz path.
- **B.3 Firefox** (`full_game mobile_team display_fit --project=firefox`, Firefox 151 via firefox-1532): **5 passed (52 s)**. Route audit `--routes-only` on the local dev server: `VIOLATIONS: 0` — every public route with zero console errors / page errors / failed requests.
- **B.3 WebKit** (same specs, WebKit 26.5 via webkit-2311): `display_fit` **3 passed**; **`full_game` and `mobile_team` failed** at the manager fixture's `expect(getByLabel(/^Rock$/i)).toBeVisible()` — the failure snapshot shows `/manager/create` with the heading and the decade checkboxes but **no genre tiles and no presets**. Route audit `--routes-only` on WebKit: `VIOLATIONS: 0` (a direct load of every route is clean). Isolation (scratch `webkit-vt-probe.mjs` / `-probe2.mjs`, local dev server **and** prod): a direct load of `/manager/create` renders all 18 checkboxes and `Rock` resolves; **Home → "Host a game" crashes the WebKit page ("Page crashed") every time the view transition runs** — default context and iPhone 12 emulation, local and prod — while the same click under `reducedMotion: "reduce"` (the hook bails out) or with `document.startViewTransition` removed lands normally with 18 checkboxes, and Home → Join / Home → Display transition without incident. Not the #222 YouTube gap and not a browser-matrix P3: the trigger is `startViewTransition` on one route → **F-04 (P1)**; the further isolation is in Part E / F.
- **B.4** `grep -n "no_more_songs" ManagerConsolePage.test.tsx` → line 1765 (`shows a clear toast when the song pool is exhausted (RpcError no_more_songs)`) plus lines 1771/1775; it ran green inside A.1.

## Part C — prod protocol

- **C.1** `post_deploy.sh` → `PASS game=ETY4ZR` (health → create → Alpha + Bravo join → end, `ended_at=2026-09-22T04:30:32Z`).
- **C.2** `buzzer_recovery.sh` → `PASS game=VVJXDE`, all 8 steps (lock atomic, release re-arms, round 2, ended).
- **C.3** prod Realtime smoke — first run: the round itself completed (create via the UI, two joins, start, race → `locked-other,winner`) but the spec's `finally` cleanup threw `getaddrinfo ENOTFOUND api.www.soundclash.org`: `deriveApiBase()` in `tests/e2e/smoke/prod_realtime.spec.ts` only maps the apex `https://soundclash.org`, and this machine must use the `www.` host. Game `NBUM9Q` was left `playing` and was ended by hand with its token (`status: ended` at 04:32:40Z). Rerun with `API_URL=https://api.soundclash.org BASE_URL=https://www.soundclash.org` → **1 passed (8.6 s)**. Not a redesign finding — every frozen locator on Join / Create / Console / Team resolved — but recorded as **F-03** (harness, P3).
- **C.4** `api-matrix.sh` → **`ALL PASS (44 cases)`**, game `J4XYYG` created and ended by the script. Every case in the plan's list passed: validation errors for slugs / empty list / 31-char / whitespace names, bidi override stripped, `not_found` for an unknown code, same-name reclaim returns the same id, bonus `unauthorized` ×2 and `validation_error` for 0 and 51 with 50 accepted, rejoin-token gating + round-trip (same id, score 50, bogus → `not_found`), `buzz_in` on `waiting` → `locked=false`, `manager_token_required`, `no_buzz_to_score`, atomic lock (A wins, B sees A, foreign id → false), `wrong_buzz_with_correct`, `+10`, `title_already_claimed`, release re-arms, free guess `0` then `−3`, `peek_next_song` one row, `extend_game` +1 h, kick → 204 and the kicked team can no longer buzz, round 2, end auth, `ended`, `gone`, `game_ended` ×2, `buzz_in` after end → false.
- **C.5 rate limiter** — 11 `POST /games` in a burst at 04:39:15–37Z: creates #1–#10 succeeded, **create #11 → `rate_limited`** (`RATE-LIMIT observation: rate_limited seen = 1`), so the per-IP limiter keyed on `CF-Connecting-IP` (PR #231) is active from this machine; every created game was ended by the script (leftovers query → 0 rows).
- **C.6 load** — self-check `node tests/load/loadtest.mjs smoke --pace fast` (04:42:43Z): game `9GE8C2`, 1 × 3 teams × 11 rounds, every flow once incl. a kick, **`LOADTEST COMPLETE verdict=PASS`**, errors 0, violations 0, final scores verified. 30-team run `final-1x30` (1 × 30 × 15, realistic pace, seed 404, launched detached 04:44:47Z, game `35RE2G`): **verdict WARN — advisory only**: invariants **PASS, 0 violations** (every race one winner, every ledger delta matched, 15/15 rounds `previous+1`), game completed and ended, **Realtime misses 0/1888**, subscriptions **32 ok / 0 failed**, unexpected errors 0 of 306 actions, 30/30 final scores verified. The single WARN is `rpc:buzz_in p95 384 ms` (p50 182, p99 553 — 30 devices buzzing in the same instant from one machine, vs 269 ms in the July `check4-1x30`); `award_attempt` p95 88 ms, `select_next_song` p95 117 ms, Realtime lock_set / round_insert / score_update p95 636 / 575 / 565 ms — all within the July envelope. `pg_stat_activity` idle+active 12 → 17 (mid-run) → 18 (post), no leak. Ledger row added to `tests/load/FINDINGS.md`. The harness drives no browser, so this number cannot be a redesign effect; the browser-side buzz path is measured in D.4.
- **C.7 No leftovers** — at 04:51:28Z `select … from active_games where status <> 'ended' and expires_at > now()` → **0 rows**.
- **C.7** _(pending)_

## Part D — prod real-browser game

Three scripted games on `https://www.soundclash.org` through the deployed UI (host at 1280×800, Display at 1920×1080, six iPhone-12 player contexts + six REST teams + FV-13 mid-game; scratch `prod-game.mjs`, Appendix D with three harness corrections listed below). Every game was ended by the script; D.9 confirms nothing is left.

| run | game | outcome |
|---|---|---|
| 1 | `QPGQFC` (05:01–05:03Z) | S4–S19 PASS; S1/S2/S3/S16/S20 reported FAIL only because the script's `isVisible({timeout})` never waits (verified live: the token-less console shows "You're not the host of this game." within 1 s, the bad code shows "does not exist", the End dialog opens and confirms); S7 see below |
| 2 | `3RMQJB` (05:08–05:09Z) | S1–S6 PASS with the waits fixed; S7 as below; aborted at S8 because round 2 came up as a **soundtrack** round (one reveal row) which the script did not anticipate — ended cleanly |
| 3 | `PXFRNX` (05:17–05:19Z) | **S1–S19 PASS**, S20 PASS on every assertion except the plan's own copy line (see below) |

Plan-text corrections (not app findings): **S7** expected `continue-round` disabled once both tokens are claimed — `docs/game-rules.md` lines 128–129 say Continue is disabled only when *no buzz is held*, and after the artist claim FV-02 still holds the floor, so the app is right (the local scenario 16 passes because it releases the lock first); **S20** expected "This cannot be undone." — that is `ConfirmDialog`'s default body; the console's End dialog passes its own message ("No more rounds can be played and teams will see the final scoreboard.") and the confirm worked; **S8/S12/S18/S19** advance through a random pick, so the script now advances again when a soundtrack round comes up before a two-token scenario.

- **D.1 consolidated**: S1 create via Hot Now + Soundtracks + Israeli Pop + 80s ✓, token-less console rejected ✓ · S2 Display code entry, "Waiting for teams", QR link ✓ · S3 six UI joins + six REST joins, 5 rows + `+7 more teams playing`, `#k` / `0 pts` chips, bad code ✓ · S4 Start after `data-ready`, every phone `idle`, both YouTube layers visible ✓, exactly one infinite animation (the pulse) with `data-round-live` ✓ · S5 six-way race → exactly 1 `winner` + 5 `locked-other`, console status + Display banner name the winner, timer 1–10 ✓ · S6 Correct Song +10, floor kept, chip/reveal/DB/phone/board agree ✓ · S7 Continue → all idle, FV-02 wins, Correct Artist +5, both claimed, Continue stays enabled while the floor is held ✓ · S8 Next round → masked rows, open chips, no `???` ✓ · S9 Wrong ×2 → −3 −3, lock cleared each time ✓ · S10 +10 / free-guess 0 / −3 / +5 ✓ · S11 Bonus picker 12 buttons, `Award +4 bonus to FV-12`, +4 ✓ · S12 held buzz + Next round → score unchanged ✓ · S13 soundtrack: badge, single button, one reveal row, +15, work name revealed ✓ · S14 Hebrew: console line Hebrew, Display row revealed with `dir="auto"`, not clipped, Secular One/Heebo loaded ✓ · S15 reloads keep identity/floor/round/board ✓ · S16 backup host link `#mt=` usable, rescue `#rt=` lands with FV-07's score, same-name join reuses FV-08's id (12 teams) ✓ · S17 kick → 204, tab redirected, picker 11, row gone ✓ · S18 `extend_game` +1 h with no banner, `+7 more` recomputed, FV-13 wins a race ✓ · S19 skip round with no score change; latency probe ✓ · S20 End → `FINAL RESULTS` + `WINNER` on host, Display and phones, exports present, `final-scoreboard-more`, DB `ended`, late join "already ended" ✓.
- **D.3 console/network**: `errors=0 csp=0 failed=0` for host, stranger, display, FV-01…FV-06, host-recovered, FV-07-rescued, FV-08-again in every run; fonts served 200 + immutable. The only non-zero lines are harness/expected: `FV-13` `failed=4/5` = requests of the Home page aborted by the script's immediate second `goto` (`ERR_ABORTED`), and `badcode` / `late-join` `errors=1` = Chromium's own "Failed to load resource: 404/410" line for the expected `not_found` / `gone` responses that the UI then renders. No `pageerror`, no CSP line anywhere.
- **D.4 buzz latency** (FV-01's tab, prod): run 1 `pointerdown→pending 1.9 ms`, `pointerdown→buzz_in request 2.8 ms`, rtt 81 ms; run 3 `2.1 ms`, `3.1 ms`, rtt 77 ms → medians ≈ 2.0 ms / 3.0 ms against the 16 ms / 5 ms budget. `git log main -- BuzzButton.tsx useBuzzer.ts` shows no redesign commit.
- **D.5 Grafana**: Tempo (`{ name = "game.buzz.e2e" } | quantile_over_time(duration, .5, .95)` via the datasource proxy, max 25 h per query): validation window 04:00–06:00Z → 19 spans, **p50 319 ms / p95 511 ms**; the seven days before Task 7 merged (2026-09-11…17) hold **no buzz spans at all** (no browser game was played that week), the nearest reference is 2026-09-20 (253 spans, p50 362 / p95 945 ms) — this run sits inside/below it. Loki: `realtime_fanout` events present for every game (`fanout_ms` 387–417 on the round-advance seen across host, display and phones); `stale_buzz_lock_resynced` **4 lines, expected 0** → **F-10** (observation, below); no `error` lines.
- **D.6 DB cross-check**: `QPGQFC` scores FV-02 20 / FV-01 17 / FV-04 10 / FV-12 4 / five at 0 / FV-05 −3 / FV-13 −3 / FV-03 −6 = the script's ledger line for line (FV-06 kicked); rounds 1–7 `closed=true`, round 8 open at End, round 4 (soundtrack) `title` and `artist` true, round 2's `artist_claimed_by` is null only because FV-06 was deleted (FK `SET NULL`); `status='ended'`. `PXFRNX` likewise (FV-02 20 … FV-13 −3; rounds 1–7 closed, 8 open; ended 05:18:54Z).
- **D.7 Sentry**: not checked — maintainer (no Sentry access from this session).
- **D.8**: the two-phone game is in the device checklist above.
- **D.9**: `active_games … name like 'FV-%' and status <> 'ended'` → 0 rows; no non-ended game at all at 05:20Z.

## Part E — design conformance

- **E.0 `node tests/smoke/ui_prod_pass.mjs all`** (04:52:31–04:53:24Z): **`PASS 29/29 hard gates green`, exit 0, zero soft findings** — deploy markers (`theme-color #14120e`, `--accent:#ff7a00`, `--bg:#14120e`, Anton + Instrument Sans in the CSS, no `bg-drift`, no `#f8fafc`), fonts 200 + immutable, console clean on desktop1280 / desktop1920 / display1920 / manager390 / reducedMotion, no CSP violations, animations only transform/opacity/filter/clip-path/visibility, no horizontal overflow, display 1920×1080 fits with 4 rows × 3 spans and the Hebrew reveal not clipped, manager 390×844 all action buttons above the fold with no vertical scroll, route transitions Home → Host → back → Join, reduced motion nothing running longer than 1 ms.
- **E.1 static routes** (scratch `redesign-audit.mjs` + `audit-lib.mjs`, Chromium, prod, 6 routes × 4 viewports + reduced motion + view transitions; token expectations follow 01 §2.1 as amended by 09): **89 checks OK**. Violation lines and their triage:
  - `tokens: --accent-ink='#000' want #000000` on every route — the shipped shorthand is the same colour; audit normalised, **no finding**.
  - `hover-touch: tap-highlight rgba(0,0,0,0.18) / rgba(51,181,229,0.4) on a` at `/` — Home's "How to play →" link has no `-webkit-tap-highlight-color` reset (Android's blue flash on tap) → **F-05**.
  - `tap-targets: 111×24 a` at `/` on both phone viewports — the same link is 24 px tall → **F-06**.
  - `contrast: 3.94:1 (min 4.5) '−3' span.chip` on `/how-to-play` — `--negative` on `--negative-soft` → **F-07**.
  - `reduced-motion: 5 animation(s)` on `/` and `/how-to-play` — all five are the `rise` entrance keyframes collapsed to 0.01 ms by the global rule, `playState: finished`, `fill: both` (they stay listed forever); **0 running > 1 ms**, the same gate `ui_prod_pass.mjs` applies and the reading the 2026-09-20 pass already adjudicated as not a finding. Audit now counts moving animations. **No finding.**
  - `view-transition`: **correction (post-merge review)** — the script's own transition leg never completed: its Home → Play selector `/play/i` matches the "DISPLAY" card since the 09 Home redesign, so the leg timed out on `/display` and the script exited before printing `VIOLATIONS:` (pre-merge and post-merge alike; the selector is fixed in the scratch script now, and the wordmark assertion dropped because Host create and Join intentionally render no `<Logo>`). The behaviour itself is proven elsewhere: `ui_prod_pass.mjs`'s "route transitions: Home → Host → back → Join" gate passed 29/29 both times, `redesign_guards.spec.ts` asserts Home → Host starts a transition, and the post-merge subagent re-ran all three Home legs with exact card names on Chromium: `startViewTransition` called exactly once per leg, `scrollY` 0 after, Back instant (counter unchanged), zero console errors — **OK**. `fonts`, `focus-ring` (every tab stop shows a ≥ 2 px outline), `gradients`, `backdrop-filter`, `transition-all`, `resting-shadow`, `emoji`, `copy` (Home h1/subhead/link order, Join rejoin hint) — **OK on every route and viewport**.
- **E.2 in-game states** (`AUDIT` lines from the three games): Display `waiting` / `13-teams` / masked rows — OK; Team `waiting` `infinite=4` (the wordmark's four bars, allowed off-round) → `playing-idle` **`infinite=1`** (the pulse, with `data-round-live="true"`) → `winner` / `locked-other` / `ended` OK; host `playing-idle` / `buzzed` / `claimed-both` / `soundtrack` / `ended`: `hover-touch` on the toast's close button (`Toast.module.css .close`, no tap-highlight reset) → **F-05**, `contrast 4.40:1` on the Wrong button's label and points → **F-07**; Display `buzzed` / `claimed-*` / `soundtrack` / `hebrew`: `'1'`/`'2'` rank numeral at 5.87:1 against the audit's 7:1 floor — it is the **buzzed row's** numeral, drawn in `--accent` on the `--accent-soft` row (`.bigRowBuzzed .bigRank`), ≥ 3:1 for a display-size numeral → **F-08 (P3)**; Display `claimed-both` `'2s' timerValue 4.40:1` (the low countdown in `--negative`) → **F-07**; all three roles `ended`: `h2 "Top teams"` set in the UI caption style rather than Anton → **F-09 (P3)**. Fit: host **390×844, 375×667, 360×640 OK** (every score button, Continue/Next and End game above the fold, no scroll); Display **1920×1080 and 1280×720 OK** with 12 teams (5 rows, `+7 more teams playing`, rows inside the viewport, no scroll). Display contract: `main[data-density="normal"]`, `ol[style*=--rows]`, every `li[data-team-id][data-rank]` with exactly three spans, no literal `???` — OK in every state.
- **E.3 prod artefacts**: `index-TBFd5Vnn.js` references `DisplayPage-DLuFfpnK.js`, `ManagerConsolePage-Bx55kyZG.js`, `TeamGameplayPage-Dar2IVVN.js`, `useGameChannel-DAmMvcMI.js`; `startViewTransition` appears once, in the eager index (the hook) and in no lazy chunk; `framer-motion|lenis|gsap` 0; the literal `auto-animate` is minified away everywhere (0 even in DisplayPage) so the sourcemap-based `check-bundle.mjs` from A.1 is the guard that counts. `/fonts/anton-latin.woff2`, `instrument-sans-latin.woff2`, `secular-one-hebrew.woff2`, `heebo-hebrew.woff2` → `200`, `font/woff2`, `Cache-Control: public, max-age=31536000, immutable`; `index.html` preloads the display face (`as="font" type="font/woff2" crossorigin`); `<meta name="theme-color" content="#14120e">`, `<meta name="color-scheme" content="dark">`; `manifest.webmanifest` `theme_color` = `background_color` = `#14120e` (the plan's `#000000` predates 09/#327). CSP: no `font-src`, so fonts ride `default-src 'self'` — no CSP line in any run.
- **E.4 looked-at-it review** (screenshots read one by one):
  - *Shell*: flat warm-black ground, no gradient or blob, cards on `--surface` with a 1 px hairline and no shadow, primary buttons solid orange with black text, inputs on `--surface-2` with the orange focus outline (Display's code field shows it). **Matches.** Disabled primaries (Join / Open / Create with nothing entered) render at 40 % as specified — not a colour deviation.
  - *Home* (1280 and iPhone): hero wordmark centred (mark beside the text on desktop, stacked on the phone), "Name the song. Buzz first." in bone, three colour-coded cards with 3 px left rules and matching icons, 3-up on desktop and icon+text rows on the phone, "How to play →" as a quiet text link, composition vertically centred with no dead band. **Matches 09.**
  - *How to play*: SVG schematic (host phone with ABCDEF and score tiles, TV with board + QR, two player phones with BUZZ tiles), Roles cards with line icons, numbered steps with the vertical rule and SET UP / PLAY labels, speaker icon on the audio note, scoring chips, FAQ list, Back link. **Matches**; the `−3` chip is F-07.
  - *Join* (iPhone, `/join/ABCDEF`): centred column, no card, six-cell code field in the display face with `6/6` and `0/30` tabular counters, prefilled code muted, full-width Join game with Cancel as a ghost link below it (05 §2 says "above"; the built order — primary first, ghost under it — is the Task 5 as-built and reads better on a phone; noted, no finding), rejoin hint verbatim. **Matches.**
  - *Host create* (iPhone): Quick-start chip row scrolling horizontally without a scrollbar, genre toggle tiles two-up with the check box, "(0 selected)" in the title, decades as one segmented control, sticky bottom bar with Cancel ghost + Create game. **Matches.**
  - *Team*: waiting = `--surface-2` fill, muted WAITING + "for the game to start", wordmark centred in the top row between the identity and standing chips; idle = flat orange, black BUZZ in Anton at the clamp size, chips on translucent surface top-left/right; winner / locked-other tones per 01 §2.2. **Matches.**
  - *Console* (1280 and 390×844): one-row header (large tabular code chip, PLAYING pill, round counter right), two icon+text ghost utilities, player strip, song title in the display face (Hebrew falls through to Secular One) with the artist in the text face, SONG OPEN / ARTIST OPEN chips, status strip, 2×2 grid (Correct Song / Correct Artist green outline, Wrong red outline, Bonus bone outline), Continue + Next 2-up, End game as a red ghost. **Matches**; the status strip renders "FV-03buzzed in — score it:" with no visible gap between the name and the verb → **F-11 (P3)**.
  - *Display*: two-column grid, wordmark + very large code chip, orange banner with the team name in the display face and the round pill, masked reveal rows (note/mic icons + five blocks), token chips, 4 px orange countdown bar with seconds, QR on a light inset with code + URL, rank rings (orange ring on the buzzed row), names in the display face, bone scores, "+7 MORE TEAMS PLAYING". **Matches**; soundtrack state shows one film-icon row, Hebrew state renders the title right-to-left inside the LTR card without clipping.
  - *Final Results* (all three roles): three podium columns with the centre tallest, line-icon crown and WINNER caption, rank rings, TOP TEAMS table (5 rows) + "…and 7 more teams", export buttons on the console. **Matches**; no confetti, no ★ glyph.
  - *Toasts / dialogs*: the End-game dialog is a `--surface` panel on a 70 % backdrop with the End game button; toasts are `--surface` cards with a tone bar (seen in the console runs). **Matches.**
  - *Admin* `/admin/songs`: not visited (inherits only; `admin_songs_crud` passed in B.1).
- **E.5 motion frames**: the D.1 script's fixed offsets (120 / 180 / 420 ms after the action) all landed **before** the Realtime fan-out (~400 ms), so `display-buzz-banner-mid.png`, `display-reveal-mid.png` and `display-reorder-mid.png` show the pre-state, not the animation. Retaken on two short 2-team prod games (`2BF3DF`, `VDT92D`, scratch `motion-frames.mjs`): keyed on the DOM change and, on the second run, with every running animation **paused at a fixed offset** before the screenshot (a headless screenshot lands ~100 ms after the signal, past most of a 250 ms keyframe). Results: `display-buzz-banner-frozen-100ms.png` — `banner-in` (250 ms) mid-slide, the bar translucent and still rising (row 19: transform only, never squashed); `display-reveal-frozen-150ms.png` — `unmask` (400 ms) mid-wipe with the title clipped to "Too Swe" (row 21: clip-path, not a fade) **and** three 400 ms WAAPI row moves from auto-animate in flight, Bravo's row overlapping Alpha's as they swap (row 22); the un-frozen `display-reveal-t0.png` catches the score count-up at 9 → 10 (row 23) and the point pill's `point-out`. The E.1 log confirms `/team/<CODE>` while `playing` has exactly one infinite animation (the pulse) and the wordmark bars are paused (`[data-round-live="true"]`).
- **E.6 performance budget**:

| Metric | Value | Budget / baseline |
|---|---|---|
| Eager JS gzip (`index` + `vendor`) | 62.70 + 74.14 = **136.84 KB** | baseline 135.08 KB, budget ≤ +20 KB → **+1.76 KB** |
| CSS gzip (all chunks) | **19.61 KB** | baseline 20.93 KB, budget ≤ +10 KB → **−1.32 KB** |
| Fonts | **68,724 B**, 4 woff2, `swap`, display face preloaded, immutable | ≤ 120 KB |
| Largest raster in `frontend/public` | none over 200 KB (`find … -size +200k` → empty; the OG card is 88.8 KB after #327) | no new raster > 200 KB |
| Bundle guard | auto-animate only in `DisplayPage-*.js`; no framer/lenis/gsap | pass |
| Buzz path (prod, browser) | pointerdown → pending **2.0 ms**, → `buzz_in` request **3.0 ms** (medians of 2) | < 16 ms / < 5 ms |
| Tempo `game.buzz.e2e` | p50 319 ms / p95 511 ms (19 spans, this window) | no pre-Task-7 spans exist; 2026-09-20: p50 362 / p95 945 |
| Lighthouse | **skipped** (left to the maintainer in DevTools; no accessibility baseline was ever recorded, so this run records none) | — |

## Screenshots (scratchpad, not committed)

Under `C:/Users/yulin/AppData/Local/Temp/claude/C--Users-yulin-GBA-Sound-Clash/a9021511-95d0-4210-a5d2-4ae72454d5b1/scratchpad/`:

- `shots-run1/static-<route>-<viewport>.png` — E.1: `/` (file `static-_-…`), `/how-to-play`, `/join`, `/join/ABCDEF`, `/manager/create`, `/display` at 1280×800, 1920×1080, iPhone 12, 360×640 (full page). Reviewed one by one: Home ×2, How to play, Join (phone), Create (phone), Display entry.
- `shots/` (run 3, `PXFRNX`) — `host-{waiting,playing-idle,buzzed,claimed-title,claimed-both,soundtrack,hebrew,ended}.png`, `host-buzzed-{390x844,375x667,360x640}.png` (#177 fit), `display-{waiting,13-teams,playing,buzzed,claimed-title,claimed-both,masked,soundtrack,hebrew,ended}.png`, `FV-01-{waiting,playing-idle,locked-other,hebrew,ended}.png`, `FV-03-winner.png`; `shots-run1/` and `shots-run2/` hold the same set for the earlier games.
- `shots-motion/` (`2BF3DF`, `VDT92D`) — `display-buzz-banner-frozen-100ms.png` (banner mid-slide), `display-reveal-frozen-150ms.png` (title mid-wipe + rows mid-swap), `display-reorder-frozen-180ms.png`, `display-reveal-t0.png` (count-up at 9 → 10), plus the `-t0`, `-t100`, `-t150`, `-t350`, `-end` stills.
- `shots/webkit-direct-create.png` — the create page in WebKit after a direct load (renders normally), part of the F-04 isolation.

Load-test ledger: the `final-1x30` row was appended to `tests/load/FINDINGS.md`, which is deliberately untracked (`.gitignore:66`, PR #280) — the numbers are in Part C.6 above.
